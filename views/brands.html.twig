<html>
<head>
    <title>Shoe Store</title>
</head>
<body>
    <div id ="wrapper">
        <h1>Available Brands</h1>
            {% if brands is not empty %}
                <ul>
                    {% for brand in brands %}
                        <li><h4><a href="/brand/{{ brand.getId }}">{{ brand.getName }}</a></h4></li>
                    {% endfor %}
                </ul>
            {% endif %}
            <br />

            <form action'/brands' method='post'>
                <label for='brandname'>Brand Name</label>
                <input id='brandname' name='brandname' type='text'>

                <button type='submit' class="btn btn-default">Add Brand</button></br>
            </form>

                <button type="button" class="btn btn-default"><a href='/stores'>See All Stores</a></button>
                <br>
                <button type="button" class="btn btn-home"><a href='/'>Home</a></button>
    </div>
</body>
</html>
